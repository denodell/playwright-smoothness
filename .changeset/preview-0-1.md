---
'playwright-smoothness': minor
---

First preview. Quick-mode measurement (Event Timing and Long Animation Frames) with `smoothness.measure()`, baselines stored per test, label, mode and CPU model, and the `toBeSmooth()` matcher with `enforce: 'warn'` by default. Results are written as JSON (`schemaVersion: 1`) under `test-results/smoothness/`.
